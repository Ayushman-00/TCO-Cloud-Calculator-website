apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tco-cloud-calculator-chart.fullname" . }}-test-connection"
  labels:
    {{- include "tco-cloud-calculator-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tco-cloud-calculator-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
